{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run climate_search",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/climate_search",
        "args": [], 
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "build climate_search",
        "MIMode": "gdb",
        "miDebuggerPath": "/usr/bin/gdb",
        "console": "integratedTerminal"
      }
    ]
  }
  